export type Option = { value: string; label: string }

export interface Meta {
  readonly id: number
  name: string
}
export enum Title {
  'Mr' = 'Mr',
  'Mrs' = 'Mrs',
  'Ms' = 'Ms',
}

export enum Currency {
  CHF = 'CHF',
}
